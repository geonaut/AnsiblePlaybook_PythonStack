---

- name: Install Python core
  apt: name={{ item }} update_cache={{ update_apt_cache }} force=yes state=installed
  with_items:
    - python-dev
    - python-pip
  tags: packages

- name: Install global python packages
  pip:
    name: virtualenv
